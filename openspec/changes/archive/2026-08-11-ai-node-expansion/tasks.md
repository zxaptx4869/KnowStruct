## 1. 数据模型与迁移

- [x] 1.1 为 `directory_draft` 新增可空 `target_node_id` 列（FK `nodes.id`，`ON DELETE SET NULL`）与索引，编写 Alembic 迁移并完成真实 MySQL 验证
- [x] 1.2 删除节点时，将指向该节点的活跃拓展草稿置为已放弃（复用 `discard_active_draft` 思路），补充回归测试

## 2. 后端：目标子树生成与差异

- [x] 2.1 扩展草稿创建接口：`POST /api/projects/{project_id}/drafts` 接受可选 `target_node_id`，非空时创建节点级拓展草稿，复用单活跃草稿并发校验
- [x] 2.2 组装拓展上下文：项目背景 + 目标节点现有子树（含说明）+ 项目内 Source 摘要（限长），编写节点级目标树生成提示词（输出目标节点下的 children 数组，要求完整保留现有子树）
- [x] 2.3 目标树结构化解析与校验：≤6 层、同级标准化名称唯一、名称 1-100、说明 ≤1000，非法输出标记草稿失败并给出可读原因
- [x] 2.4 实现递归差异计算（新增/保留/建议移除，按标准化名称匹配），草稿 payload 返回 `target_node_id` 与差异快照；会话应用目标树后刷新差异
- [x] 2.5 会话式微调适配：`apply_directory_tree` 提交目标节点 children 目标树，复用意图折叠、10 轮压缩、30 轮上限与有界自愈
- [x] 2.6 确认合并：单事务内创建新增节点（复用深度/重名/归属校验）、删除勾选的移除子树（复用受保护删除校验），任一失败整批回滚，重复提交幂等；目标节点已删除时拒绝并置草稿已放弃
- [x] 2.7 后端自动化测试：正常生成/差异/确认、空子树、非法树失败与重试、受保护移除阻断、单活跃草稿并发、目标节点删除失效、跨 Workspace 安全

## 3. 前端：入口与差异确认面板

- [x] 3.1 节点详情页 node-hero 编辑按钮旁新增「AI 拓展建议」图标入口（桌面与 390px 移动端），存在活跃草稿时隐藏
- [x] 3.2 扩展 draft 类型与 queries：`target_node_id`、差异快照与相关 mutation
- [x] 3.3 `DraftPanel` 拓展模式：递归差异树（新增/保留/建议移除），移除默认不勾选、受保护项禁用并显示阻断数量，保留项默认折叠
- [x] 3.4 差异面板与会话区联动：会话应用后刷新差异，确认按钮执行合并并展示结果/回滚错误
- [x] 3.5 前端组件测试与既有测试更新，`npm run lint && npm run build` 通过

## 4. 验证、同步与收尾

- [x] 4.1 全量验证：后端 `pytest` + `ruff`，前端测试/lint/build，`openspec validate --all --strict`
- [x] 4.2 浏览器验收（桌面 1440 + 移动 390）：发起拓展、差异确认（含受保护阻断）、会话微调、单活跃草稿并发、手动创建放弃草稿
- [x] 4.3 同步主规格（`directory-draft`、`knowledge-directory` 与新增 `node-expansion`）并归档 change
- [ ] 4.4 推送合并前与用户确认
