## 1. 设计 token 与全局样式

- [x] 1.1 按设计稿"轻量 UI 规范"更新 `frontend/src/index.css` 的 `@theme`：主色 `#2767D8`（hover `#1f58bd`）、页面背景 `#eef1f4`、主表面、文字三级层级、语义色（成功/警告/危险）、4-8px 圆角基线
- [x] 1.2 收敛 `index.css` 中散落的硬编码色值到 token，统一按钮/输入框/状态标签/菜单/弹窗的圆角与文字层级
- [x] 1.3 核对并落地桌面布局尺寸：全局导航侧边栏、项目工作区目录面板与内容区比例（目录面板约 280px），与设计稿一致

## 2. 登录页

- [x] 2.1 按新 token 细化登录页视觉（默认 / 表单错误 / 登录中），桌面与 390px 移动视口均通过；不实现认证前的数据面板
- [x] 2.2 更新 `LoginPage.test.tsx` 中受样式/文案影响的断言并保持通过

## 3. 项目列表页

- [x] 3.1 页头移除"个人工作区"小标签和"每个项目独立维护知识目录。"说明；"创建项目"按钮改小，桌面右上角、移动端右上角小按钮（不再占整行）
- [x] 3.2 无项目空状态改为说明 + 指向右上角"创建项目"的引导文案，移除"创建第一个项目 / 前往采集箱"按钮
- [x] 3.3 `ProjectDialog` 把"项目目标"和"背景"合并为单个"项目目标与背景"字段（最多 500 字符，提交到 `goal`），前端不再发送/展示 `background`
- [x] 3.4 删除项目确认弹窗标题与按钮统一为 `删除"X"项目？` / `删除项目`；移动端项目卡片节点数文案统一为"X 个目录节点"
- [x] 3.5 更新 `HomePage.test.tsx` 以覆盖新的空状态、页头与弹窗字段行为

## 4. 项目与知识目录工作区

- [x] 4.1 顶栏移除"新建节点"主按钮；"项目设置"改为图标按钮；计数统一为"X 个目录节点"
- [x] 4.2 目录面板头部去掉副标题，保留"知识目录"标题与创建根节点图标（aria：创建根节点）
- [x] 4.3 节点头部去掉"知识节点"小标签，按钮统一为"编辑节点"（次要）与"创建子节点"（唯一主操作）；空节点内容区不再重复放置按钮
- [x] 4.4 `NodeDialog` 移除"节点说明"字段（创建与编辑均只保留名称），标题与"上级节点"文案按设计稿统一
- [x] 4.5 `MoveNodeDialog` 去掉头部小标签；节点菜单与按钮文案统一为"创建子节点"
- [x] 4.6 空目录 / 空节点说明文案统一为"手动创建节点，不会自动采用 AI 目录。"，空目录操作统一为蓝色主按钮
- [x] 4.7 移动端：顶栏改为"状态 + 标题一行、节点数第二行"；节点头部增加节点菜单入口（⋯），复用现有节点菜单能力；验证"上一层"逐级返回
- [x] 4.8 更新 `ProjectDetailPage.test.tsx` 与相关组件测试以覆盖上述行为

## 5. 验证与收尾

- [x] 5.1 前端验证：`cd frontend && npm test -- --run && npm run lint && npm run build`
- [x] 5.2 浏览器真实验收：在 1440×900、1280×800、390×844 三视口检查登录、项目列表、工作区的主要状态（默认/空/失败/弹窗/拖拽），并核对移动端布局不重叠不溢出
- [x] 5.3 `openspec validate --all --strict` 通过
- [x] 5.4 按归档流程同步 delta spec 到主规格并归档本 change
- [x] 5.5 提交前端代码、测试与规格变更到当前分支
