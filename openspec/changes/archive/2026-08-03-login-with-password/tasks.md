## 1. 数据库与配置基础

- [x] 1.1 补充异步数据库 Engine、Session factory、请求级 `get_db` 依赖和应用关闭清理，并为测试提供可替换的数据库依赖。
- [x] 1.2 初始化 Alembic 环境，使迁移从应用配置读取 MySQL URL 并加载统一 SQLAlchemy metadata。
- [x] 1.3 实现 `User`、`Workspace`、`AuthSession` 模型及其唯一约束、外键和 Token/过期时间索引。
- [x] 1.4 生成并审查首个认证迁移，分别验证空数据库 upgrade、downgrade 和再次 upgrade。
- [x] 1.5 增加 Argon2id 密码哈希依赖，移除未使用的 JWT 依赖与配置，并补齐会话期限、Cookie、可信 Origin 和登录限流环境变量示例。

## 2. 账号预置与认证领域服务

- [x] 2.1 实现账号标识规范化、12 至 128 字符密码校验、Argon2id 哈希验证，以及随机 Session Token 和数据库摘要工具。
- [x] 2.2 实现会话创建、查询、24 小时/30 天固定过期、当前会话撤销、用户全部会话撤销和过期拒绝服务。
- [x] 2.3 实现交互式创建账号 CLI，在单个事务中创建唯一用户和默认“我的工作区”，并确保明文密码不进入参数、输出或日志。
- [x] 2.4 实现交互式管理员重设密码 CLI，成功后撤销目标用户全部会话，并正确处理账号不存在和密码不合规。
- [x] 2.5 为账号重复、事务回滚、密码边界、哈希验证、Token 只存摘要、会话过期和撤销编写后端单元测试。

## 3. 认证 API 与安全边界

- [x] 3.1 定义登录请求、当前用户/默认工作区响应和稳定认证错误 Schema。
- [x] 3.2 实现 `POST /api/auth/login`，覆盖统一凭据错误、两种 Cookie 期限和默认每 IP 每分钟 10 次的可配置限流。
- [x] 3.3 实现 `GET /api/auth/me` 和 `POST /api/auth/logout`，确保无效会话清 Cookie、退出幂等且撤销后的 Token 不可复用。
- [x] 3.4 实现 `get_current_user` 与默认工作区上下文依赖，并明确健康检查/认证端点公开、业务端点受保护的路由接入方式。
- [x] 3.5 实现非安全 HTTP 方法的可信 `Origin` 校验，以及开发/生产 Cookie 安全配置校验。
- [x] 3.6 编写认证 API 集成测试，覆盖成功、统一错误、限流、Cookie 属性、24 小时/30 天期限、当前用户、401、Origin 拒绝、退出和密码重设撤销。

## 4. 前端登录与会话恢复

- [x] 4.1 扩展前端 API 客户端统一携带 Cookie，并提供可区分凭据错误、未认证和其他服务错误的稳定错误对象。
- [x] 4.2 实现认证状态层及启动时 `/api/auth/me` 恢复流程，检查期间使用稳定加载状态且不渲染受保护内容。
- [x] 4.3 根据 D00/M00 实现共用响应式 `/login` 页面，包括账号、密码、密码可见性、保持登录和无注册边界文案。
- [x] 4.4 实现登录表单必填校验、提交中防重复、统一错误、账号保留和成功状态转换。
- [x] 4.5 实现受保护路由和公共路由守卫，安全保存站内原目标并在成功登录后返回，拒绝外部跳转目标。
- [x] 4.6 在桌面全局导航和移动端“我的”入口接入退出操作，成功或会话已失效时均清理状态并返回登录页。
- [x] 4.7 配置前端测试工具并覆盖表单状态、会话恢复、401 跳转、已登录访问登录页、原目标返回和退出流程。

## 5. 验收与文档同步

- [x] 5.1 使用真实 MySQL 测试库运行迁移、CLI 和后端测试，并通过 Ruff 或项目约定的后端检查。
- [x] 5.2 运行前端 lint、自动化测试和生产构建，修复所有认证变更引入的问题。
- [x] 5.3 使用预置测试账号在桌面与 390px 移动视口完成登录、错误、保持登录、刷新恢复、受保护直达和退出端到端验收。
- [x] 5.4 验证浏览器存储和数据库中没有明文密码或原始 Session Token，并验证生产 Cookie、Origin 与限流配置失败时能阻止不安全启动或请求。
- [x] 5.5 更新 README、环境变量说明和 OpenSpec 前置基线中的账号预置与会话决定，并将功能结构图的 P0“注册 / 登录 / 退出”修正为“已有账号登录 / 当前用户 / 退出”。
