## 1. 数据模型与迁移

- [x] 1.1 新增 `directory_drafts` 与 `directory_draft_nodes` 模型（状态、意图说明、勾选、树关系）
- [x] 1.2 增加「同项目至多 1 条非终态草稿」的数据库约束
- [x] 1.3 编写 Alembic 迁移并在真实 MySQL 验证

## 2. 后端：AI 调用与提示词

- [x] 2.1 实现 DeepSeek/豆包 `generate_outline`（背景 + Source 摘要 + 澄清答案 → 候选树）
- [x] 2.2 实现澄清问题生成（充分性判断 + ≤5 个结构化问题，可跳过）
- [x] 2.3 实现增量微调调用（草稿 + 意图说明 + 意见 → 改动清单，未提及原样保留）
- [x] 2.4 实现意图说明维护（确认后并入，AI 浓缩失败回退追加，用户可编辑）
- [x] 2.5 起草/澄清/微调输出结构化校验（深度、重名、名称长度、说明长度）
- [x] 2.6 起草与微调走异步任务（可离开页面，失败可重试，幂等不重复）

## 3. 后端：API 与状态机

- [x] 3.1 起草 API（创建草稿、触发生成、返回状态）
- [x] 3.2 澄清提交与跳过 API
- [x] 3.3 候选编辑（勾选/改名/删除）与微调意见提交 API
- [x] 3.4 确认采用 API（事务创建节点，复用 nodes 校验，整批回滚，幂等）
- [x] 3.5 放弃/重试/重新起草 API，并执行「单草稿」与「手动创建作废草稿」并发规则
- [x] 3.6 草稿查询与「草稿待确认」入口 API（Workspace 隔离）

## 4. 后端：自动化测试

- [x] 4.1 起草、澄清、微调、确认、放弃、重试、重新起草的正常与失败场景
- [x] 4.2 非法 AI 输出、超层、重名、空树校验与幂等重试
- [x] 4.3 单草稿约束、手动创建作废草稿、确认事务回滚、跨 Workspace 隔离
- [x] 4.4 运行 `cd backend && .venv/bin/pytest -q && .venv/bin/ruff check .` 全绿

## 5. 前端：起草面板

- [x] 5.1 空目录态「AI 起草目录」入口与「草稿待处理」入口（有草稿时隐藏起草按钮）
- [x] 5.2 起草面板：澄清步（问题卡片、可跳过）→ 起草进度 → 候选预览/编辑（勾选、内联改名、删除、默认全选、祖先自动包含）
- [x] 5.3 调整意见框 + 重新生成（增量微调），意图说明可查看/编辑
- [x] 5.4 确认采用 / 放弃草稿 / 重新起草操作与反馈
- [x] 5.5 手动创建节点时若有草稿，弹出放弃确认
- [x] 5.6 移动端 390px 全流程可用、不横向溢出

## 6. 前端：自动化测试

- [x] 6.1 起草入口显示/隐藏、澄清跳过、预览编辑、微调、确认、放弃场景
- [x] 6.2 手动创建作废草稿、草稿待确认入口恢复、移动端布局
- [x] 6.3 运行 `cd frontend && npm test -- --run && npm run lint && npm run build` 全绿

## 7. 全量验证与文档同步

- [x] 7.1 运行 `openspec validate --all --strict` 通过
- [x] 7.2 使用 demo 账号在桌面 1440 与移动 390 验收起草全流程（含失败/重试/并发冲突）
- [x] 7.3 同步 delta spec 到 `openspec/specs/knowledge-directory/spec.md` 并新增 `openspec/specs/directory-draft/spec.md`
- [x] 7.4 归档 `ai-directory-draft` 并提交分支（推送/合并前先经用户确认）
